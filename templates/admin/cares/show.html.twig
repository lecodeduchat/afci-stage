{% extends 'base.html.twig' %}

{% block title %}Cares{% endblock %}

{% block body %}
    <h1>Cares</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ care.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ care.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ care.price }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ care.duration ? care.duration|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ care.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cares_index') }}">back to list</a>

    <a href="{{ path('cares_edit', {'id': care.id}) }}">edit</a>

    {{ include('cares/_delete_form.html.twig') }}
{% endblock %}
