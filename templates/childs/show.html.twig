{% extends 'base.html.twig' %}

{% block title %}Childs{% endblock %}

{% block body %}
    <h1>Childs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ child.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ child.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ child.lastname }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ child.birthday ? child.birthday|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_childs_index') }}">back to list</a>

    <a href="{{ path('app_childs_edit', {'id': child.id}) }}">edit</a>

    {{ include('childs/_delete_form.html.twig') }}
{% endblock %}
