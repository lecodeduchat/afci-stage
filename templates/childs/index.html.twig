{% extends 'base.html.twig' %}

{% block title %}Enfants
{% endblock %}

{% block body %}
	<h1>Vos enfants</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Prénom</th>
				<th>Nom</th>
				<th>Date de naissance</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for child in childs %}
				<tr>
					<td>{{ child.firstname }}</td>
					<td>{{ child.lastname }}</td>
					<td>{{ child.birthday ? child.birthday|date('Y-m-d') : '' }}</td>
					<td>
						<a href="{{ path('app_childs_show', {'id': child.id}) }}">show</a>
						<a href="{{ path('app_childs_edit', {'id': child.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="5">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a href="{{ path('app_childs_new') }}">Create new</a>
{% endblock %}
